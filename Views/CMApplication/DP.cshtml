@model IEnumerable<CMWebAPI.Models.CMApplication>

<h2>Applications</h2>



<table class="table">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>AdminCategories</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CI_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdminCategories)
            </td>
        </tr>
    }
</table>